// Jenkinsfile for building cray-node-identity
@Library('dst-shared@master') _
sle15CN {
    channel = "skern-build"
    specfile  = "cray-node-identity.spec"
    timeout = 5
    product = 'shasta-premium'
    slackNotification = ["", "", false, false, true, true]
}

